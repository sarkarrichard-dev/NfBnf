"""Delta Exchange India REST — signed client, contract master, market data.

Ported from ``reference/openalgo/broker/deltaexchange/`` (the vendored OpenAlgo
submodule). That directory is the source of truth for any Delta protocol detail
— check it before changing signing, an endpoint, or a payload shape.
"""
